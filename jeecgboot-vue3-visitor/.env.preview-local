# 本地 preview-local 模式专用（仅 `pnpm preview:local` 加载，不会污染 `pnpm build` 真生产）
# 使用方式：pnpm preview:local  → vite build --mode preview-local && vite preview
#
# 与 jeecg-boot/application-dev.yml 中 jeecg.cs.crypto 完全一致，方便本地 backend 联调
# 此文件不影响真生产镜像；真生产走 .env.production 中的 prod key
VITE_PUBLIC_PATH=/cs/userChat/

VITE_GLOB_DOMAIN_URL=/jeecgboot
VITE_GLOB_API_URL=/jeecgboot
VITE_GLOB_API_URL_PREFIX=

# dev key（与 .env.development 一致）
VITE_CS_STORAGE_KEY=PKHX8dfuePB2PpT4
VITE_CS_STORAGE_IV=7JBzAcDRQc9HCahm
VITE_CS_TRANSPORT_KEY=Cdg9VObOpE3yEQzz
VITE_CS_TRANSPORT_IV=frYwJYzoqXvv5ePy

VITE_BUILD_COMPRESS=gzip
VITE_BUILD_COMPRESS_DELETE_ORIGIN_FILE=false
