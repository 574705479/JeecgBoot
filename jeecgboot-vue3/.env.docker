# 是否启用mock
VITE_USE_MOCK = false

# 发布路径
VITE_PUBLIC_PATH = /

# 是否启用gzip或brotli压缩
# 选项值: gzip | brotli | none
# 如果需要多个可以使用“,”分隔
VITE_BUILD_COMPRESS = 'gzip'

# 使用压缩时是否删除原始文件，默认为false
VITE_BUILD_COMPRESS_DELETE_ORIGIN_FILE = false

#后台接口父地址(必填)
VITE_GLOB_API_URL=/jeecgboot

# 后台基址（浏览器可访问）。勿填容器内主机名(jeecg-boot-system 等)，否则静态资源 URL 打进 JS 后无法解析并触发 HTTPS 混合内容；与 production 一致用相对路径，运行时会拼成当前站点 origin
VITE_GLOB_DOMAIN_URL=/jeecgboot

# 接口父路径前缀
VITE_GLOB_API_URL_PREFIX=


# 填写后将作为乾坤子应用启动，主应用注册时AppName需保持一致（放开 VITE_GLOB_QIANKUN_MICRO_APP_NAME 参数表示jeecg-vue3将以乾坤子应用模式启动）
#VITE_GLOB_QIANKUN_MICRO_APP_NAME=jeecg-vue3
# 作为乾坤子应用启动时必填，需与qiankun主应用注册子应用时填写的 entry 保持一致
#VITE_GLOB_QIANKUN_MICRO_APP_ENTRY=//qiankun.boot3.jeecg.com/jeecg-vue3

# 全局隐藏哪些布局。可选属性：sider,header,multi-tabs；多个用逗号隔开
#VITE_GLOB_HIDE_LAYOUT_TYPES=sider,header,multi-tabs

# 在线文档编辑版本。可选属性：wps, offlineWps(离线版), onlyoffice
VITE_GLOB_ONLINE_DOCUMENT_VERSION=wps

# 客服加解密（与生产后端一致；若仅本地联调 dev 后端，请整段注释以使用代码内 dev 默认密钥）
VITE_CS_STORAGE_KEY=K9mP2xQ7nR4vW8sL
VITE_CS_STORAGE_IV=T3yB6cF1hJ0kN5zX
VITE_CS_TRANSPORT_KEY=Z4aS8dG2fH7jM1qW
VITE_CS_TRANSPORT_IV=E5rT9vY3uI6oP0xC
